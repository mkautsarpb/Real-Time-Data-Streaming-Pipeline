version: 2

models:
  - name: stg_books
    description: "Clean staging model for books data"
    columns:
      - name: book_title
        tests:
          - not_null


  - name: my_second_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null
